{
  "name": "essay-gated-mint",
  "version": "1.0.0",
  "private": true,
  "description": "Participation-gated mint: essay score ≥90 gets 10 tokens. Single campaign, 21k cap.",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.ts",
    "deploy:8004": "hardhat run scripts/deploy-8004.ts",
    "verify": "hardhat run scripts/verify.ts",
    "backend": "ts-node backend/src/server.ts",
    "scoring:test": "ts-node backend/src/scoring/score.ts",
    "frontend": "cd frontend && npm run dev",
    "mcp": "cd mcp && npm run dev",
    "mcp:build": "cd mcp && npm run build",
    "mcp:http": "cd mcp && npm run dev:http"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "^25.1.0",
    "hardhat": "^2.19.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "ethers": "^6.9.0",
    "express": "^4.18.2"
  }
}
